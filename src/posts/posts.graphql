type Post {
  id: Int!
  title: String!
  content: String!
  author: String!
}

type Query {
  posts: [Post!]!
  post(id: Int!): Post
}

type Mutation {
  createPost(title: String!, content: String!, author: String!): Post!
  deletePost(id: Int!): Boolean!
}
